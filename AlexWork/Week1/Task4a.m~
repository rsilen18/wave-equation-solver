% Visualization of numerical Solution for BVP for 1-D Wave Equation:
%     Interval: x in (0,1)
%     u(x=0,t) = 0
%     u_x(x=1,t) = 0
%     Initial Value: u_0 = sin(3pi/2*x)
%     Initial Velocity: u_
% Author: Alexandre Ait-Ettajer
% Date: May 19, 2020 

N = input('N: ');
M = input('M: ');
k = input('k: ');
t_f = input('Final Time: ');
a = 0;
b = 1;
c = 1;

x = linspace(a,b,(N+1));
dx = (b-a)/N;
dt = t_f/M;
assert(x(2)-x(1) == dx);

u_old = 
u_cur
u_new

for n = 1:M
   for j = 1:N+1
       if (j == 1)
           

       elseif (j == N+1)
           
       else
           
       end
   end
end